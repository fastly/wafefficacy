id: cmdexe-false-positive

info:
  name: Command Execution
  author: wafefficacy
  severity: info
  tags: cmdexe,false-positive

http:
  - payloads:
      cmdexe: false-positives.txt.encoded

    raw:
# GET doesn't add much value in this benchmark
#      - |
#        GET /vulnerabilities/exec/?ip={{cmdexe}}{{suffix}} HTTP/1.1
#        Host: {{Hostname}}
#        Connection: close

      - |
        POST /vulnerabilities/exec/ HTTP/1.1
        Host: {{Hostname}}
        Content-Type: application/x-www-form-urlencoded
        Connection: close

        ip={{cmdexe}}{{suffix}}

    matchers:
      - type: status
        status:
          - 1
        negative: true
