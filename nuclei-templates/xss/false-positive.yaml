id: xss-false-positive

info:
  name: Cross Site Scripting (XSS)
  author: wafefficacy
  severity: info
  tags: xss,false-positive

http:
  - raw:
      - |
        GET /vulnerabilities/xss_r/?p={{url_encode(xss)}}{{suffix}} HTTP/1.1
        Host: {{Hostname}}
        Connection: close

      - |
        POST /vulnerabilities/xss_r/ HTTP/1.1
        Host: {{Hostname}}
        Content-Type: application/x-www-form-urlencoded
        Connection: close

        p={{url_encode(xss)}{{suffix}}}

    payloads:
      xss: xss-false-positives.txt

    matchers:
      - type: status
        status:
          - 1
        negative: true
